#include<stdio.h>
#include<stdlib.h>
struct point {
int x;
int y;
int z;
};
struct point* fun(int a, int b, int z){
struct point *ptr=(struct point*)malloc(sizeof(structpoint));
ptr->x=a;
ptr->y=b+5;
ptr->z=d-4;
return ptr;
}
void print (struct point *ptr) {
print("%d %d %d\n", ptr->x, ptr->y, ptr->z);
}
int main(){
struct point *ptr1, *ptr2;
ptr1 = fun(5,4);
ptr2 = fun(6,8);
ptr3 = fun(9,3,4);
print(ptr1); print(ptr2); print(ptr3);
free(ptr1);
free(ptr2);
free(ptr3);
return 0;
}
